<snippet>
	<content><![CDATA[
if(${1:<condition>})
	${2:<commands>}
${3:elseif(${4:<condition>})
	${5:<commands>}}
${6:else()
	${7:<commands>}}
endif()
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>if</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.cmake</scope>
</snippet>
